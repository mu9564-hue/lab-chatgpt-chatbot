# Todo 앱

간단한 Streamlit 기반 Todo 애플리케이션입니다.

실행 방법:

```bash
pip install -r web/requirements.txt

# Streamlit 버전 (기존)
streamlit run web/todo_app.py

# Flask + 정적 프론트엔드 (웹)
python web/app.py

# 브라우저에서 열기: http://localhost:5000
```

데이터는 `web/todo_data.json`에 로컬로 저장됩니다.
